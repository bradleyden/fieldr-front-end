<nav class="banner navbar navbar-default">
  <div class="banner container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
        <li class='nav-link'>{{#link-to "games"}}Games{{/link-to}}</li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li class='nav-link'>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li class='nav-link'><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li class='nav-link'>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li class='nav-link'>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

{{#if isAuthenticated}}
<div></div>
{{else}}
{{jumbo-page}}
{{/if}}

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>
