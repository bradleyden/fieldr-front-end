
import { sfTable } from 'fieldr-front-end/helpers/sf-table';
import { module, test } from 'qunit';

module('Unit | Helper | sf table');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sfTable([42]);
  assert.ok(result);
});

